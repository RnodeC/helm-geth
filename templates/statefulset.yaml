---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: geth
  labels:
    app: geth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: geth
  template:
    metadata:
      labels:
        app: geth
    spec:
      serviceAccountName: geth
      volumes:
       - name: jwt
         secret:
           secretName: jwt
      containers:
      - name: geth
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: IfNotPresent
        command: 
        - geth
        args:
        - --mainnet
        - --http
        - --http.addr=0.0.0.0
        - --http.port=8545
        - --authrpc.addr=0.0.0.0
        - --authrpc.port=8551 
        - --ipcdisable
        - --datadir=/chaindata
        - --authrpc.jwtsecret=/etc/eth2/token.jwt
        ports:
          - name: rpc
            containerPort: 8551
            protocol: TCP
          - name: http
            containerPort: 8545
            protocol: TCP
          - name: ipc
            containerPort: 30303
            protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: http
        readinessProbe:
          httpGet:
            path: / 
            port: http
        volumeMounts:
        - name: chaindata
          mountPath: /chaindata
        - name: jwt
          mountPath: /etc/eth2
  volumeClaimTemplates:
  - apiVersion: v1
    metadata:
      name: chaindata
      labels:
        app: geth
    spec:
      storageClassName: "{{ .Values.storage.storageClassName }}"
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: "500Gi"

